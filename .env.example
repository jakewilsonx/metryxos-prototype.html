# Supabase — Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_5_ffAkCOPoj5pl6IizZAzA_TfYdRptF
# Service role key — server-only, never expose to the client. Used for
# org bootstrap, invite acceptance, and scripts/seed.ts.
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic — used server-side only (API routes), never in client code
ANTHROPIC_API_KEY=
git clone <repo> && cd metryxos-prototype.html
git checkout claude/metryxos-build-xumcuu
npm install
cp .env.example .env.local
